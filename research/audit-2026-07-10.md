# Project audit (2026-07-10) — 6-agent cleanup backlog

> **EXECUTION STATUS (2026-07-10):** P0 ✅ · P1 ✅ · P2 ✅ · P3 ✅ (banners done; physical reorg +
> e5-name rename deferred as optional) · P4 ✅ (EN/ES desync fixed; attack-count recorded for the
> author-gated paper pass) · P5 ✅ (Rajaram-2014 editor set corrected; missing bib entries deferred to
> the E5-SP write-up; Vickrey eponym / ES summary bands left as cosmetic). All cleanup committed +
> pushed. Remaining genuinely-deferred (author-gated / lower-value): the v2 mechanism propagation to the
> paper, the opportunity-cost hurdle refinement, and the optional research/ physical reorg.

Synthesis of a 6-agent read-only audit (numbers, EN↔ES mirror, citations, corpus organization,
roadmap/governance, code). **Headline: the corpus is fundamentally SOUND** — cross-references clean
(`scripts/check-anchors.mjs`: broken=0, dead-paths=0, dead-wikilinks=0), attacks/defenses complete and
paired (A001–A043 ↔ D001–D043, docs/67–113), paper bibliography symmetric (80 entries EN/ES, 0 orphans),
the current engine (`e5-sp-model.mjs`) correct + deterministic. **Most defects are self-inflicted by the
last-hour 3.0× correction** (headers/banners updated, bodies not) plus accreted disorder. Priority order
below; work top-down.

## P0 — Reconcile the half-applied 3.0× correction (self-inflicted; CRITICAL; cheap; do FIRST)
The current "authoritative" files contradict themselves — a reader (or the author applying the
propagation note) gets the retracted 2.0×/1.43× picture.
1. **`research/e5-sp-paper-propagation.md` body vs its own header.** Header (3.0×/1.30×/35%) vs the
   section-by-section "Ready text" (~2.0×, ×1.43, "best-vs-mediocre"). It is the designated *single
   bilingual source* → applying it verbatim injects stale numbers into the paper. Rewrite the body.
2. **`research/e4e5-value-model-v2.md` build-results block (lines ~149–201) + Parts 5–6.** Still say
   ~2.0×, "selection 1.37× × delivery 1.43×", "harm-blindness inert / 99% oracle", "best-vs-mediocre",
   "net-neg <1% / 98% positive", "delivery 1.43× unchanged". Contradicts the MAJOR CORRECTION block
   above it. Update or banner as superseded-by-correction.
3. **`research/e5-sp-preregistration.md` held-out table is NON-REPRODUCIBLE.** The "7/7 PASS / validated"
   table is the OLD config; rerunning the note's exact command now gives ρ=1→2.04×, delivery 1.30×,
   ρ=0→4.19×, tornado 2.90×. **Regenerate the held-out confirmation on the corrected defaults**
   (mean=0.06, fWeak=0.75, fVer=0.975) and restate which predictions pass; P1 "parity at ρ=1" is now
   FALSE (2.04×, not the 1.30× floor) — reword to "compresses toward the floor as ρ→1". Until then the
   ~3.0× is a *recalculation, not a re-run* — do not call it "validated as pre-registered".
4. **`scripts/e5-sp-model.mjs`:** (a) header prints literal `(1.43x)` — compute it (=1.30×);
   (b) `--tornado` knobs sweep OUTSIDE the calibrated regime (mean [0.30,0.55], fVer [0.78,0.90]) →
   they HIDE the net-neg sensitivity the correction introduced; fix to mean∈[0.0,0.15], fVer∈[0.90,0.99],
   relabel `/0.75`; (c) `--cats` corr(S,P) column is a hardcoded `?` — compute or drop; (d) add an
   `oracle>0` assertion (parity with the pipeline).
5. **Band floor:** "~3.0× (2.6–3.6×)" tracks the low-corr (independent-influence) end; at corr=0.3 the
   cell is ~2.2–2.4×, below the 2.6× floor. Restate the band honestly (or note it tracks the
   low-corr reading).
6. **Open mechanism-fidelity refinement (already at roadmap top of stack):** the 35% is modeled as all
   true-harm (S<0) via low `mean`, over-reviving harm-blindness; the faithful split is ~8% true harm +
   ~27% below-opportunity-cost (needs the hurdle). Magnitude ~3× robust; mechanism split pending.

## P1 — Metadata drift (external-facing; recurrence of C026; cheap)
`CITATION.cff` (version "v1.8" + date 2026-07-04, self-inconsistent with its own "Working paper v1.12"
note), `.zenodo.json` (title + version v1.8), `README.md:37` (v1.8 DOI 21228492) all lag the paper's
**v1.12**. Bump all to v1.12; confirm the preferred-citation DOI (21252911) points at the current deposit;
add the version bump to a release checklist so it can't drift again (C026 already "corrected" once).

## P2 — Roadmap restructure (the author's explicit request)
`docs/03_ROADMAP.md` is 990 lines, INVERTED (≈331 lines of changelog first; live stack at 702–975);
Phase 0–9 template systematically stale (calls done/published work unstarted); three competing "next"
anchors (337–352, 702–950, 952–975); factual errors ("35/35"→43/43 at :957; "v1.6"→v1.10 at :361);
old-multiplier sections (2.09/1.83/2.2×, §781/§844) unbannered. Restructure:
- `docs/03_ROADMAP.md` (live, ~150–200 lines): ONE prioritized **ACTIVE STACK** at top → a truthful
  program-status table (replacing Phases 0–9) → deferred/candidate studies → standing invariants (the
  "Preserve A0xx" block, moved out of "next steps") → conventions.
- `docs/03_ROADMAP_HISTORY.md` (archive): the changelog (1–331) + the superseded 2.2×-era sections.
- `docs/04_DOC_INDEX.md` (new): master map of docs/00–113 (type + status); note the C026–C045 audit-queue
  home so the lone C040 (docs/104) isn't read as a numbering gap.
Also fix: `defenses/README.md:100` intro ("no accepted resolutions yet" vs rows resolved in docs/105–109);
README resolution ranges →67–113 (attacks:11 "67-109", defenses:18 "67-103"); attacks header "(A041)"→
"(A041–A043)".

## P3 — Corpus organization: banner superseded + tidy outputs
`research/` is 50 files flat; only `e4-analytical-backbone.md` has a supersession banner. Add the same
banner (pointing to `e4e5-value-model-v2.md`) to the ~11 unmarked superseded notes:
`e4e5-analytical-propositions.md`, `e4e5-pipeline-preregistration.md`, `e4e5-confirmation-bias-design.md`
(premise FALSIFIED by the code-review synthesis), `e4e5-propositive-review-synthesis.md`,
`e4e5-partisan-bias-note.md`, `e4e5-polarization-note.md`, `e4-constructive-review-synthesis.md`,
`e4-theoretical-positioning.md`, `e4-propagation-and-decisions.md`, `e4-v3-preregistration.md`,
`e4-v3-gamma-literature.md`, `e4-calibration-targets.md`, `e4-institutional-knowledge-design.md`,
`e4-v2-fair-comparison-design.md`. Keep reachable (still cited by v2): `e4-v5-capture-design.md`,
`value-model-core-v0.md`. Then: (optional) the canonical/e-ladder/archive/results reorg; fix the E5 name
collision (`e5-value-delivery-*` vs `e5-sp-*`); stale outputs — `e4e5-pipeline-run.json` is a stray
deltaPartisan=100 run the pipeline OVERWRITES every run (write to a gitignored/scratch path);
`e4e5-pipeline-results-corrected-1M.txt` uses a retired output format; add a `research/README.md` listing
the canonical set. VERIFY: auditor-4 flagged `drafts/paper.md` cites `research/e4-v4-symmetric-frontier.md`
(missing), but check-anchors reports dead-paths=0 — reconcile.

## P4 — Paper mirror + internal staleness (bilingual; some author-gated)
- **One real EN↔ES desync:** the "aggregated allocation profiles, not a central plan" reframing of the
  passive-share default reached EN but not the ES *body* — `paper.es.md:3` (header clause), §4 (:460–464),
  §5.3 (:608–609). (The ES summaries already carry the new framing.) Fix the three ES spots together.
- **Stale attack narrative (both langs):** paper says "forty attacks / four rounds / docs 67–109" but
  cites docs/111–113 (A041–A043). Update count/range in lockstep.
- **DEFERRED (author-gated):** the full harm-blindness→agenda-capture / ~2.2×→~3.0× mechanism propagation
  to the paper (EN+ES together, at a deliberate version bump; then a Zenodo go). See P0.1 — the
  propagation note body must be de-staled BEFORE this.

## P5 — Minor
- `MEMORY.md` index omits `paper-goal-plan.md`, `user-accessibility.md` (add or mark archived).
- Verify **Rajaram et al. (2014)** editor set in `public-investment-efficiency-loss-calibration.md:324`
  (Rajaram-Le-Biletska-Brumby matches their 2010 WP; the 2014 book may credit a different editor list).
- Missing full bib entries for Gilens-Page/Achen-Bartels/Mayhew/Arnold/WSJ (only in working notes; add
  when the E5-SP paper is written up — the published paper is clean).
- Vickrey (1961) uncited-except-as-eponym; "Offermann 2026b" with no 2026a; ES summary bands (2–2.7×/2–3×)
  not pinned to the paper's figure; MEMORY.md index summaries (v1.8, "~1.7×") stale.
