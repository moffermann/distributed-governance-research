# Codex critique — full-paper E4 coherence (Track 1 / B)

Repo: master, commit ba67122+. Context: E4 v1.14 (a harm-myopia four-scenario robustness map) was integrated into
`drafts/paper.md` + `drafts/paper.es.md` and you already judged that integrated subsection publication-ready. A
coherence pass then threaded the v1.14 map through the rest of the manuscript so it is part of the CLAIMED result,
not an orphan subsection: version header bumped to v1.14 + a reconciliation clause; the "What survives" summary
gained a point (1b); the findings table E4 row cites the v1.14 map; the appendix "E4 calibration targets" points to
the anchored scenarios. The symmetry-gate framing (Δ≈0.025 NO-GO) is intentionally KEPT — it is the no-myopia anchor
of the map. "Finding 4" (the separate E4-v4/v5 institutional-knowledge experiment) is intentionally left intact.

## Task — assess ONLY E4 narrative coherence (not a re-review of the blessed subsection or the non-E4 paper)
Read every E4 mention in `drafts/paper.md` (search: "Δ = 0.025", "retire", "rests on the architecture", "Finding 4",
"What survives", "four-scenario", "v1.14", "E4 calibration", the §6 block, findings table) and the mirrored
`drafts/paper.es.md`. Judge, under the comparative-institutions robustness bar (NOT calibrated impact):
1. **Coherence:** does the manuscript now read as ONE consistent E4 story — the pre-registered gate = the no-myopia
   near-parity regime; the v1.14 map showing where each institution wins under realistic harm-myopia — with no
   place that CONTRADICTS the map (e.g. a bare "the paper rests only on architecture/mechanism, modest result" that
   ignores the map) and no double-counting or result-shopping?
2. **No new overclaim / no multiplier notation** introduced by the coherence edits (the pre-existing retired
   2.19×/2.22×/2.26× "apparatus output, not calibrated" framing is fine and must stay labelled as such).
3. **EN↔ES faithful** for every coherence edit (header, What-survives (1b), findings row, appendix note); 17/17
   section parity.

Then the verdict: **is the full-paper E4 narrative now coherent and publication-ready?** YES/NO. If NO, list ONLY
the specific remaining contradictions/overclaims with file:line + exact fix.

## Output
`audits/2026-07-11-v1.14-design/CRITIQUE_coherence_SUMMARY.md`: explicit YES/NO; remaining issues if any; EN/ES note.
