# E4 form-pass — panel CONSENSUS (draft for ratification)

All six panelists SUPPORT the restructure (headline the probable E4 scenario in the body; full comparative analysis
to a single Appendix E4). The conditions were convergent across the pro-Core-v0 / neutral / pro-central spectrum.
This synthesis is the agreed plan; ratify or object with ONE specific fix.

## Acceptance test (panel-method, adopted): the SINGLE-EYEFUL + SWAP tests
A body-only reader must, in one eyeful of the E4 headline capsule, come away with ALL of:
1. under the source-motivated probable scenario, coverage-routed distributed selection is ahead (~91% vs ~45%);
2. the magnitudes are DECLARED reference points, not calibrated effects (with the transport-gap + inner-MC + harm-
   myopia-is-load-bearing caveats — panel-skeptic's four);
3. a competent, harm-aware central TIES (~91% vs ~85%, the pre-registered near-parity gate, Δ≈0.025, NO-GO) — the
   honest reference for a capable state (panel-statist's hard red line);
4. at the central's own declared favourable endpoint the ranking REVERSES and the central WINS (~98% vs ~68%).
SWAP test: the structure must be defensible if the winner were reversed — "probable" leads only because it is the
declared modal/source-motivated case, not because it favours the thesis.

## Consensus BODY capsule (replaces the ~34-line scenario wall + the redundant gate restatement)
> E4 maps *where each institution wins*, not one calibrated number; every magnitude below is a **declared,
> source-motivated reference point, not a calibrated effect** (its central inputs are proxy-informed with an
> unpropagated political-opinion→project-value transport gap, and its intervals cover inner Monte-Carlo variability
> only). Under the **probable** scenario — a central that reads visible, credit-bearing value but is *assumed* myopic
> to concentrated harm on the low-visibility long tail — coverage-routed distributed selection delivers about **91%**
> of the full-information benchmark against the central's **~45%** (a **+46.6-point gap**). That gap rides on the
> harm-myopia assumption, the model's **load-bearing, least-empirically-pinned knob** (≈40% of the gap): make the
> central competent and harm-aware and the arms **tie** (~91% vs ~85% — our pre-registered near-parity gate,
> Δ≈0.025, NO-GO), and at the central's own **declared favourable endpoint the central wins** (~98% vs ~68%). The
> full four-scenario map, the diagnostic contrast, the frontier (parity at t≈0.57 of the declared segment), the
> benchmark theorem, and the four limits are in **Appendix E4**.

## FORM plan (relocation + tightening; ZERO information loss)
- **Dedupe the "declared reference point, not calibrated" hedge** (repeated ~6× in the E4 block): keep it ONCE in the
  body capsule and ONCE in Appendix E4 (panel-editor).
- **Relocate the full gate methods** (paper.md ~L570–620: K=500, estimand, λ/ρ/h grid, decision rule) VERBATIM to
  Appendix E4; leave a ≤4-sentence synopsis in the body (recovers ~45 lines, loses no fact) (panel-editor).
- **Merge into ONE numbered "Appendix E4: four-scenario robustness map"** — extend the existing calibration-targets
  appendix (L1469); do NOT create a second, disconnected appendix (panel-lib/inst). It reproduces: the 5-row
  scenario table (lift from `research/e4-paper-section-draft.md`), the 40/60 decomposition, the t≈0.57 frontier, the
  theorem, and the four limits — complete.
- **Split the run-on abstract E4 sentence** (L13) into shorter sentences; keep it two-sided (panel-method/skeptic).
- **Quarantine "probable"**: bind it once as "source-motivated reference, not a calibrated posterior" (panel-skeptic).
- **Keep in the BODY**: the NO-GO Δ≈0.025 reconciliation, the frontier t≈0.57, and the "who-wins-where" three facts
  (statist). Do NOT sever +46.6% from the NO-GO reconciliation (panel-method's result-shopping red line).
- **Invariants**: no ×/D-C multiplier notation; EN↔ES faithful mirror (17/17 parity); relocate, never delete.

## Union of RED LINES (any violated ⇒ the restructure is NOT honest)
central-wins endpoint (98/68) + central-tie (91/85, Δ≈0.025) both in body; +46.6% never readable as calibrated/
forecast; transport-gap + inner-MC + harm-myopia-load-bearing caveats co-located with the number; every appendix row
preserved; +46.6% stays tied to the NO-GO reconciliation; EN/ES caveat parity; no ×/D-C for punch; hedges relocated,
not cut.
