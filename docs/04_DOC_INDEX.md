# docs/ master index

Navigable map of the numbered `docs/` corpus (00–113). Created 2026-07-10 (audit cleanup — the only
prior index was `36_DIAGRAM_INDEX`, which covers diagrams only). Types: **framing**, **architecture**,
**C-resolution** (contradiction), **A-resolution** (attack), **interstitial** (architecture doc that
happens to sit in the resolution range). Attack↔defense↔resolution pairings are indexed in
`attacks/README.md` and `defenses/README.md`; this index is the doc-level map.

## Framing & method (00–06)
- `00` Research philosophy · `01` Research method · `02` Project scope · `03` Roadmap (live) ·
  `03_ROADMAP_HISTORY` (full pre-2026-07-10 archive) · `06` Current project recap.
- (Numbers `04`, `05` are intentional gaps — `04` is now this doc index; `05` unused.)

## Architecture & design (07–38)
Core scope and object/interface/flow models: `07` Core v0 scope · `08` Entity & role map ·
`09` Project object model · `10` Fiscalization/evidence/control model · `11` Citizen project card ·
`12` Open-project parallel closure · `13` Value-icon catalog + metric validator · `14–20` Layered
citizen interface (home/list/dashboard/signal/full-sheet/audit-trail) · `21–32` Citizen & project
flows (funding, following, fiscalizer offer, evidence, comment, complaint, delegation, auto-allocation,
creation, lifecycle, disbursement, reformulation) · `33` System v0 blueprint · `34` Core v0 scope
freeze · `35` Consolidated entity/object/state map · `36` Diagram index · `37` Scope-classification
matrix · `38` Contradiction/failure-mode checklist.

## Contradiction resolutions C001–C025 (39–63)
One doc per resolved contradiction: `39`=C001 … `63`=C025 (idea/control/complaint/funding-commitment/
public-institution/value-verification/evidence-producers/treasury/AI-assistance/state-translation/
delegation-priority/reputation-responsibility/fiscalization-cost/identity-comments/milestone-AI/value-
thesis/fulfillment-reputation/protocol-change/tutored-governance/observability-baseline/common-good/
delegation-concentration/beneficiary-privacy/discovery-customization). Note: C026–C045 are NOT docs —
they live as a consistency-audit queue in `contradictions/README.md`; the lone later C-resolution as a
doc is **C040** (`104`, closure-deadline expiry).

## Formal inventory (64–66)
`64` Formal entity inventory · `65` Responsibility matrix by role · `66` Implementable object schema draft.

## Attack resolutions A001–A043 (67–113) + interstitials
One doc per resolved attack (paired with a defense brief; see `attacks/`/`defenses/` READMEs): `67`=A001
… `84`=A018, then `86`=A019, `87`=A020, `88`=A021, `89`=A033, `90`=A022 … `100`=A032, `102`=A034,
`103`=A035, `105`=A036 … `109`=A040, `111`=A041, `112`=A042, `113`=A043. (The A-number is in each
filename; ordering is not strictly sequential after A021 because some attacks were resolved out of order.)
**Interstitial architecture docs in this range** (not attack resolutions): `85` funding-window expiry +
budget-liquidity smoothing · `101` **Core v0 minimal publishable model** (the accessible companion) ·
`104` C040 closure-deadline expiry · `110` **operating-regime ladder & semi-open mode**.

## Status
All 43 attacks (A001–A043) and 25 contradictions (C001–C025) are resolved. Superseded records (an older
project-state diagram, an earlier recap) are marked in place. Keep this index updated when a new numbered
doc is added (append the number, type, and one-line role).
